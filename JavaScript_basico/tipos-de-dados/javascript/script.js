// Boolean
// Null
// Undefined
// Number
// String
// /n Diz para ir para a próxima linha

// String (cadeia de caracteres)
var caracteres = "Palavra\nPalavra";
console.log(caracteres);

// Number(Número comun ou decimal)
var number1 = 20;
var number2 = 30;
var somar = number1 + number2;

console.log(number1);
console.log(number2);
console.log(somar);
console.log("conca" + "tenação"); // O sinal de + em uma string, siginifica que é uma concatenação (o código vai juntar as palavras "conca" e "tenação" que vai resultar em concatenação)
console.log(50 + 50); // O sinal de + em um number, irá fazer a soma (O código vai fazer 50 + 50 que resulta em 100)
console.log("vo" + 6); // O sinal de + em uma string e um number, vai fazer a concatenação (O código vai fazer "vo" + 6 que resulta em vo6)

// Undefined
var indefinido;
console.log(indefinido); // Quando não há nada na variável o código fica indefinido.

// null
var a = null; 
console.log(a); // Significa que não há nada dentro da variável

// Boolean
var a = true; // O boolean significa true ou false (verdadeiro ou falso)
console.log(a);
var b = false;
console.log(b);